apiVersion: v1
kind: Service
metadata:
    name: {{ include "app.fullname" . }}
spec:
    type: {{ .Values.service.type }}
    selector:
        app: {{ include "app.name" . }}
    ports:
        - port: 8000
          targetPort: 8000
